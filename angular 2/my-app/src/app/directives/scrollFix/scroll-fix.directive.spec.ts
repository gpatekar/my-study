import { ScrollFixDirective } from './scroll-fix.directive';

describe('ScrollFixDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollFixDirective();
    expect(directive).toBeTruthy();
  });
});
